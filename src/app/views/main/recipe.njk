{% extends '_layouts/main.njk' %}

{% block head %}
  <title>{{ recipe.name }}</title>
{% endblock head %}

{% block content %}

  <main class="container recipe">
    <div class="banner">
      <img src="{{ recipe.avatar_url }}" alt="{{ recipe.name }}">
    </div>
    <h1>{{ recipe.name }}</h1>
    <p>por {{ recipe.author }}</p>

    <section class="recipe-section">
      <h2>Ingredientes<span class="expand-button" data-js="ingredients">esconder</span></h2>

      <ul id="ingredients">
        {% for ingredient in recipe.ingredients %}
          <li>{{ ingredient }}</li>
        {% endfor %}
      </ul>
    </section>

    <section class="recipe-section">
      <h2>Modo de preparo<span class="expand-button" data-js="directions">esconder</span></h2>

      <ul id="directions">
        {% for item in recipe.steps %}
          <li>{{ item }}</li>
        {% endfor %}
      </ul>
    </section>

    <section class="recipe-section">
      <h2>Informações adicionais<span class="expand-button" data-js="note">esconder</span></h2>

      <p id="note">{{ recipe.infos }}</p>
    </section>
  </main>

  <script src="/script.js"></script>

{% endblock content %}

