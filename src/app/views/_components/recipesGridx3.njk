<div class="grid grid--recipes">

  {% for recipe in recipes %}
    <a href="/recipes/{{ recipe.id }}" class="grid__item--recipes">
      <div class="grid__image--recipes">
        <img src="{{ recipe.image }}" alt="{{ recipe.title }}">
      </div>
      <div>
        <p>{{ recipe.title }}</p>
        <small>por {{ recipe.chef_name }}</small>
      </div>
    </a>
  {% endfor %}

</div>
