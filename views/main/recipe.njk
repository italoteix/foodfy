{% extends 'layout.njk' %}

{% block head %}
  <title>{{ recipe.title }}</title>
{% endblock head %}

{% block content %}

  <main class="container recipe">
    <div class="banner">
      <img src="{{ recipe.image }}" alt="{{ recipe.title }}">
    </div>
    <h1>{{ recipe.title }}</h1>
    <p>by {{ recipe.author }}</p>

    <section class="recipe-section">
      <h2>Ingredients<span class="expand-button" data-js="ingredients">hide</span></h2>

      <ul id="ingredients">
        {% for ingredient in recipe.ingredients %}
          <li>{{ ingredient }}</li>
        {% endfor %}
      </ul>
    </section>

    <section class="recipe-section">
      <h2>Directions<span class="expand-button" data-js="directions">hide</span></h2>

      <ul id="directions">
        {% for item in recipe.preparation %}
          <li>{{ item }}</li>
        {% endfor %}
      </ul>
    </section>

    <section class="recipe-section">
      <h2>Cook's Note<span class="expand-button" data-js="note">hide</span></h2>

      <p id="note">{{ recipe.information }}</p>
    </section>
  </main>

  <script src="/script.js"></script>

{% endblock content %}

